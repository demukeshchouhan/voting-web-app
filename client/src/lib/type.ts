export type ClashData = {
  title?: string;
  description?: string;
  expireAt?: string;
  image?: string;
};

export type ClashType = {
  id: number;
  user_id: number;
  title: string;
  description: string;
  image: string;
  created_at: string;
  expire_at: string;
};
